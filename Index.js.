export default function Home() {
  return (
    <div style={{
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      minHeight: "100vh",
      background: "linear-gradient(135deg, #7D3C98, #E0E0E0)",
      color: "#fff",
      fontFamily: "Arial, sans-serif",
      textAlign: "center",
      padding: "20px"
    }}>
      
      {/* Logo */}
      <img 
        src="/portada_trendup.png" 
        alt="TrendUp Logo"
        style={{ width: "200px", marginBottom: "20px", borderRadius: "12px" }}
      />

      <h1 style={{ fontSize: "2.5rem", marginBottom: "10px" }}>
        🌟 Bienvenida a TrendUp
      </h1>
      
      <p style={{ fontSize: "1.2rem", maxWidth: "500px", marginBottom: "20px" }}>
        Tu plataforma está lista para despegar 🚀.  
        ¡Personaliza este contenido y comienza a construir tu app!
      </p>

      {/* Botón GitHub */}
      <a
        href="https://github.com/Yessik111/trendup-nextjs-starter"
        style={{
          background: "#fff",
          color: "#7D3C98",
          padding: "12px 24px",
          borderRadius: "8px",
          textDecoration: "none",
          fontWeight: "bold"
        }}
      >
        Ver código en GitHub
      </a>
    </div>
  );
}
